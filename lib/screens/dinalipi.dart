import 'package:flutter/material.dart';

class Dinalipi extends StatelessWidget {
  const Dinalipi({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('Dinalipi Content here'));
  }
}
